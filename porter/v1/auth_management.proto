syntax = "proto3";

package porter.v1;

message APITokenRequest {
  int64 project_id = 1;
}

message APITokenResponse {
  string token = 3;
}

service AuthManagement {
  // APIToken gets a Porter token for programmatic access to the Porter UI
  rpc APIToken(APITokenRequest) returns (APITokenResponse) {}
}
